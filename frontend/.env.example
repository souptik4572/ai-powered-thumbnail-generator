# Backend base URL (no trailing slash)
# Production (Heroku):
VITE_API_BASE_URL=SAMPLE_BACKEND_HOSTED_URL

# Local development — leave empty to use the Vite dev-server proxy (localhost:8000):
# VITE_API_BASE_URL=
